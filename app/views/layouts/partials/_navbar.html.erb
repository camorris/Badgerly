<!-- These lines of code Bootstrapify my navbat -->

<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand"></a><%= link_to 'Badgery', root_path, class: 'btn btn-secondary' %>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto"></ul>
            <ul class="navbar-nav">
                <%if logged_in? %>
            <li class="nav-item active">
                    <%= link_to current_user.name, user_path(current_user), class: 'nav-link' %>
            </li>
            <li class=" nav-item active">
                    <%= link_to 'View My Badges', badges_path, class: 'nav-link' %>
            </li>
            <li class="nav-item">
                    <%= link_to 'Log Out', logout_path, class: 'nav-link', method: 'delete'%>
            </li>
            <% else %>
            <li class="nav-item active">
                    <%= link_to 'Log In', new_session_path, class: 'nav-link'%>
            </li>
            <% end %>
            
            
          </ul>
        </div>
      </nav>